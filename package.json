{
	"name": "daily-logs",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "npx tsx scripts/build-static.ts",
		"dev": "npm run build && npx serve dist",
		"deploy": "npm run build && wrangler pages deploy dist --project-name=daily-logs",
		"generate-log": "./scripts/generate-log.sh"
	},
	"dependencies": {
		"gray-matter": "^4.0.3",
		"remark": "^15.0.1",
		"remark-gfm": "^4.0.1",
		"remark-html": "^16.0.1"
	},
	"devDependencies": {
		"@types/node": "^20.19.30",
		"tsx": "^4.21.0",
		"typescript": "^5",
		"wrangler": "^4.59.3"
	}
}
